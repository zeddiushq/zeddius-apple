.DS_Store
build/
DerivedData/
*.xcuserstate
xcuserdata/
.swiftpm/

# Fully generated by `xcodegen generate` from project.yml — CI regenerates it
# fresh every run, so committing it just produces merge-conflict noise on a
# machine-authored file. Run `xcodegen generate` after cloning or pulling a
# project.yml change, before opening the project in Xcode.
Zeddius.xcodeproj/

#claude
/.claude/
CLAUDE.md
